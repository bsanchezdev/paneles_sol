SELECT
	CARGA.RUT,
	CARGA.OPERACION,
	CARGA.CUOTA,
	CARGA.MONTO,
	If (
		(CARGA.CEDENTE = "BN1")
		OR (CARGA.CEDENTE = "BNF"),
		"BANEFE",
		"BANCO SANTANDER SANTIAGO"
	) AS Expr1,
	"%fecha%" AS fecha_carga
FROM
	CARGA;

